# Trust Decay Agent – Flow Diagram (PoC)

[ IoT Device Telemetry ]
          |
          v
     OBSERVE
  (Lightweight Signals)
          |
          v
     REASON
  (Trust Decay Logic)
          |
          v
   Updated Trust Score
          |
          v
       ACT
(Autonomous Response)
          |
          v
 [ Monitor | Shape | Restrict | Isolate ]

The loop runs continuously at the edge without human intervention.
Responses escalate automatically as trust decays.
